# **HALO**

HALO is a research codebase for 


## What is in this repository?
- `src/` : core source code (pipeline + mappers + utilities)
- `data/` : datasets, preprocessing outputs, and feature artifacts
- `feature_pipeline`: notebooks and scripts to construct feature sets
- `scripts/` : runnable entrypoints to reproduce key results
- `notebooks` : 
- `configs/` : configuration files for experiments
- `figures/` : exported figures for the paper
- `results/` : exported tables/metrics (generated)
- `paper/` : manuscript-related files (optional)

## Setup